diff --git a/backend/ayanavitabackend/app/api/prisma/migrations/20260226130000_add_product_images/migration.sql b/backend/ayanavitabackend/app/api/prisma/migrations/20260226130000_add_product_images/migration.sql
index 8bff3588ffeb1ec35e0062fad8196343d97e80dd..febeaceaeaf653d3aaed3e91a3a2c275cd5f04b7 100644
--- a/backend/ayanavitabackend/app/api/prisma/migrations/20260226130000_add_product_images/migration.sql
+++ b/backend/ayanavitabackend/app/api/prisma/migrations/20260226130000_add_product_images/migration.sql
@@ -1,18 +1,33 @@
-CREATE TABLE `product_images` (
+CREATE TABLE IF NOT EXISTS `product_images` (
     `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
     `product_id` BIGINT UNSIGNED NOT NULL,
     `image_url` VARCHAR(500) NOT NULL,
     `is_primary` BOOLEAN NOT NULL DEFAULT FALSE,
     `sort_order` INTEGER NOT NULL DEFAULT 0,
     `primary_flag` BIGINT GENERATED ALWAYS AS (
         CASE WHEN `is_primary` = TRUE THEN `product_id` ELSE NULL END
     ) STORED,

     INDEX `idx_product_images_sort`(`product_id`, `sort_order`),
     UNIQUE INDEX `ux_one_primary_per_product`(`primary_flag`),
     PRIMARY KEY (`id`)
 ) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-ALTER TABLE `product_images`
-    ADD CONSTRAINT `fk_product_images_product`
-    FOREIGN KEY (`product_id`) REFERENCES `products`(`id`) ON DELETE CASCADE ON UPDATE CASCADE;
+SET @fk_exists := (
+    SELECT COUNT(*)
+    FROM information_schema.TABLE_CONSTRAINTS
+    WHERE CONSTRAINT_SCHEMA = DATABASE()
+      AND TABLE_NAME = 'product_images'
+      AND CONSTRAINT_NAME = 'fk_product_images_product'
+      AND CONSTRAINT_TYPE = 'FOREIGN KEY'
+);
+
+SET @fk_sql := IF(
+    @fk_exists = 0,
+    'ALTER TABLE `product_images` ADD CONSTRAINT `fk_product_images_product` FOREIGN KEY (`product_id`) REFERENCES `products`(`id`) ON DELETE CASCADE ON UPDATE CASCADE',
+    'SELECT 1'
+);
+
+PREPARE fk_stmt FROM @fk_sql;
+EXECUTE fk_stmt;
+DEALLOCATE PREPARE fk_stmt;
diff --git a/docker-compose.yml b/docker-compose.yml
index a67cfe13ed3fdbcdd6b2087e6a566aec6af4cfdf..8af132a326d544adca97394ab67de4a71e4db797 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -5,129 +5,134 @@ services:
     command: --default-authentication-plugin=mysql_native_password
     environment:
       MYSQL_ROOT_PASSWORD: rootpassword
       MYSQL_DATABASE: ayanavita
       MYSQL_USER: ayanavita
       MYSQL_PASSWORD: ayanavita_pw
       TZ: Asia/Ho_Chi_Minh
     volumes:
       - mysql_data:/var/lib/mysql
     ports:
       - "3306:3306"
     restart: unless-stopped
     networks:
       - aya_net

   # =====================
   # BACKEND (DEV MODE)
   # =====================
   backend:
     image: node:20-alpine
     container_name: ayanavita-backend
     working_dir: /app
     volumes:
       - ./backend/ayanavitabackend/app/api:/app
       - /app/node_modules
-    command: sh -c "npm install && npx prisma migrate deploy && npx prisma generate && npx prisma db seed && npm run start:dev"
+    command: sh -c "npm install && (npx prisma migrate resolve --rolled-back 20260226130000_add_product_images || true) && npx prisma migrate deploy && npx prisma generate && npx prisma db seed && npm run start:dev"
     env_file:
       - ./backend/ayanavitabackend/app/api/.env
     environment:
       NODE_ENV: development
       PORT: 8090
     depends_on:
       - mysql
     ports:
       - "8090:8090"
     networks:
       - aya_net

   # =====================
   # FRONTEND (VITE DEV)
   # =====================
   frontend:
     image: node:20-alpine
     container_name: ayanavita-frontend
     working_dir: /app
     volumes:
-      - ./frontend:/app
       - ./frontend/landingpageayanavita:/app
       - /app/node_modules
     command: sh -c "npm install && npm run dev"
     environment:
       NODE_ENV: development
+      CHOKIDAR_USEPOLLING: "true"
+      CHOKIDAR_INTERVAL: "1000"
     ports:
       - "5174:5174"
     networks:
       - aya_net




   ayanavita-fontend:
     image: node:20-alpine
     container_name: ayanavita-fontend
     working_dir: /app
     volumes:
       - ./ayanavita-fontend:/app
       - /app/node_modules
     command: sh -c "npm install && npm run dev -- --host 0.0.0.0 --port 5178"
     environment:
       NODE_ENV: development
       VITE_API_URL: http://localhost:8090
+      CHOKIDAR_USEPOLLING: "true"
+      CHOKIDAR_INTERVAL: "1000"
     depends_on:
       - backend
     ports:
       - "5178:5178"
     networks:
       - aya_net

   frontend-final:
     image: node:20-alpine
     container_name: ayanavita-frontend-final
     working_dir: /app
     volumes:
       - ./Home/ayanavita-fontend-final:/app
       - /app/node_modules
     command: sh -c "npm install && npm run dev -- --host 0.0.0.0 --port 5176"
     environment:
       NODE_ENV: development
       VITE_API_URL: http://localhost:8090
+      CHOKIDAR_USEPOLLING: "true"
+      CHOKIDAR_INTERVAL: "1000"
     depends_on:
       - backend
     ports:
       - "5176:5176"
     networks:
       - aya_net

   # =====================
   # ADMIN (REACT DEV)
   # =====================
   admin:
     image: node:20-alpine
     container_name: ayanavita-admin
     working_dir: /app
     volumes:
       - ./backend/ayanavitabackend/app/admin:/app
       - /app/node_modules
     command: sh -c "npm install && npm run dev -- --host"
     environment:
       NODE_ENV: development
       VITE_API_BASE: http://localhost:8090
     ports:
       - "5050:5173"
     networks:
       - aya_net

   adminer:
     image: adminer
     container_name: ayanavita-adminer
     ports:
       - "8081:8080"
     networks:
       - aya_net

 volumes:
   mysql_data:

 networks:
   aya_net:
-    driver: bridge
\ No newline at end of file
+    driver: bridge
