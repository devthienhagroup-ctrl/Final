diff --git a/docker-compose.yml b/docker-compose.yml
index 94f310f11f8aaa513f080646752724babbfb7af7..edc4c8619d8e6bcf65b63691f868d6fb10e0ccd7 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -126,39 +126,51 @@ services:
       - backend
     ports:
       - "5176:5176"
     networks:
       - aya_net

   # =====================
   # ADMIN (REACT DEV)
   # =====================
   admin:
     image: node:20-alpine
     container_name: ayanavita-admin
     working_dir: /app
     volumes:
       - ./backend/ayanavitabackend/app/admin:/app
       - /app/node_modules
     command: sh -c "npm install && npm run dev -- --host"
     environment:
       NODE_ENV: development
       VITE_API_BASE: http://localhost:8090
     ports:
       - "5050:5173"
     networks:
       - aya_net

+  student:
+    build:
+      context: ./student
+      dockerfile: Dockerfile
+    container_name: ayanavita-student
+    ports:
+      - "5080:5080"
+    depends_on:
+      - backend
+    networks:
+      - aya_net
+
   adminer:
     image: adminer
     container_name: ayanavita-adminer
     ports:
       - "8081:8080"
     networks:
       - aya_net

 volumes:
   mysql_data:

 networks:
   aya_net:
     driver: bridge
diff --git a/student/Dockerfile b/student/Dockerfile
new file mode 100644
index 0000000000000000000000000000000000000000..926fa252eb123983925726bb1bf6566bab46cab6
--- /dev/null
+++ b/student/Dockerfile
@@ -0,0 +1,12 @@
+FROM node:20-alpine AS build
+WORKDIR /app
+COPY package*.json ./
+RUN npm ci
+COPY . .
+RUN npm run build
+
+FROM nginx:1.27-alpine
+COPY --from=build /app/dist /usr/share/nginx/html
+COPY nginx.conf /etc/nginx/conf.d/default.conf
+EXPOSE 5080
+CMD ["nginx", "-g", "daemon off;"]
diff --git a/student/nginx.conf b/student/nginx.conf
new file mode 100644
index 0000000000000000000000000000000000000000..49bcf65c1300980aa2825ac0e2b0fdf67b3ddfd2
--- /dev/null
+++ b/student/nginx.conf
@@ -0,0 +1,11 @@
+server {
+  listen 5080;
+  server_name _;
+
+  root /usr/share/nginx/html;
+  index index.html;
+
+  location / {
+    try_files $uri $uri/ /index.html;
+  }
+}
diff --git a/student/src/App.tsx b/student/src/App.tsx
index 3d7ded3ff62424046af18e2b5b8b8324c7e0001d..bf08c74b5e0de7af0a021f0fe5e4d6f01237256d 100644
--- a/student/src/App.tsx
+++ b/student/src/App.tsx
@@ -1,35 +1,128 @@
-import { useState } from 'react'
-import reactLogo from './assets/react.svg'
-import viteLogo from '/vite.svg'
-import './App.css'
+import { useEffect, useMemo, useState } from 'react'
+import { useAuth } from './auth'

-function App() {
-  const [count, setCount] = useState(0)
+function navigateTo(path: string) {
+  if (window.location.pathname !== path) {
+    window.history.pushState({}, '', path)
+  }
+  window.dispatchEvent(new PopStateEvent('popstate'))
+}

+function ErrorStatusPage({ code, title, message }: { code: number; title: string; message: string }) {
   return (
-    <>
-      <div>
-        <a href="https://vite.dev" target="_blank">
-          <img src={viteLogo} className="logo" alt="Vite logo" />
-        </a>
-        <a href="https://react.dev" target="_blank">
-          <img src={reactLogo} className="logo react" alt="React logo" />
-        </a>
-      </div>
-      <h1>Vite + React</h1>
-      <div className="card">
-        <button onClick={() => setCount((count) => count + 1)}>
-          count is {count}
-        </button>
-        <p>
-          Edit <code>src/App.tsx</code> and save to test HMR
-        </p>
+    <main className="status-page">
+      <p className="status-code">{code}</p>
+      <h1>{title}</h1>
+      <p>{message}</p>
+      <button type="button" onClick={() => navigateTo('/student')}>Về trang chính</button>
+    </main>
+  )
+}
+
+function LoginPage() {
+  const { token, setTokenPair } = useAuth()
+
+  const [email, setEmail] = useState('student@ayanavita.local')
+  const [password, setPassword] = useState('')
+  const [error, setError] = useState('')
+
+  useEffect(() => {
+    if (token) navigateTo('/student')
+  }, [token])
+
+  const onSubmit = (event: React.FormEvent<HTMLFormElement>) => {
+    event.preventDefault()
+
+    if (!email.trim() || !password.trim()) {
+      setError('Vui lòng nhập email và mật khẩu')
+      return
+    }
+
+    const permissions = email.includes('noperm') ? [] : ['my_courses.read']
+    setTokenPair('student-demo-token', 'student-demo-refresh-token', permissions)
+    navigateTo('/student')
+  }
+
+  return (
+    <main className="login-page">
+      <form className="login-card" onSubmit={onSubmit}>
+        <h1>Đăng nhập Student</h1>
+        <p>Đăng nhập để truy cập AYANAVITA • Student</p>
+        <label>
+          Email
+          <input type="email" value={email} onChange={(e) => setEmail(e.target.value)} />
+        </label>
+        <label>
+          Mật khẩu
+          <input type="password" value={password} onChange={(e) => setPassword(e.target.value)} />
+        </label>
+        {error ? <p className="error-msg">{error}</p> : null}
+        <button type="submit">Đăng nhập</button>
+        <small>Dùng email chứa "noperm" để mô phỏng tài khoản không có quyền.</small>
+      </form>
+    </main>
+  )
+}
+
+function StudentHomePage() {
+  const { claims, logout } = useAuth()
+
+  return (
+    <main className="student-home">
+      <div className="student-home-card">
+        <h1>AYANAVITA • Student</h1>
+        <p>Xin chào {claims?.email || 'Student'}, bạn đã đăng nhập thành công.</p>
+        <p>Portal học viên đã sẵn sàng, tích hợp kiểm tra quyền truy cập giống beay.</p>
+        <button type="button" onClick={logout}>Đăng xuất</button>
       </div>
-      <p className="read-the-docs">
-        Click on the Vite and React logos to learn more
-      </p>
-    </>
+    </main>
   )
 }

-export default App
+export default function App() {
+  const { token, can } = useAuth()
+  const [pathname, setPathname] = useState(window.location.pathname)
+
+  useEffect(() => {
+    const syncPath = () => setPathname(window.location.pathname)
+    window.addEventListener('popstate', syncPath)
+    return () => window.removeEventListener('popstate', syncPath)
+  }, [])
+
+  const view = useMemo(() => {
+    if (pathname === '/') {
+      navigateTo('/student')
+      return null
+    }
+
+    if (pathname === '/login') return <LoginPage />
+
+    if (!token) {
+      navigateTo('/login')
+      return null
+    }
+
+    if (pathname === '/student') {
+      if (!can('my_courses.read')) {
+        return (
+          <ErrorStatusPage
+            code={403}
+            title="403 - Không có quyền truy cập"
+            message="Tài khoản của bạn đã đăng nhập nhưng chưa được cấp quyền để vào trang này."
+          />
+        )
+      }
+      return <StudentHomePage />
+    }
+
+    return (
+      <ErrorStatusPage
+        code={404}
+        title="404 - Không tìm thấy trang"
+        message="Đường dẫn không tồn tại hoặc đã được thay đổi. Vui lòng kiểm tra lại đường dẫn."
+      />
+    )
+  }, [can, pathname, token])
+
+  return view
+}
diff --git a/student/src/auth.tsx b/student/src/auth.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..51cdce9ef8b657871bb85889266bacb2f3bff735
--- /dev/null
+++ b/student/src/auth.tsx
@@ -0,0 +1,62 @@
+import { createContext, useContext, useMemo, useState } from 'react'
+import {
+  ACCESS_TOKEN_KEY,
+  REFRESH_TOKEN_KEY,
+  clearTokenPair,
+  decodeJwtClaims,
+  hasPermission,
+  readAccessToken,
+  readRefreshToken,
+  writePermissionKeys,
+} from './session'
+
+type AuthState = {
+  token: string
+  refreshToken: string
+  claims: ReturnType<typeof decodeJwtClaims>
+  setTokenPair: (accessToken: string, refreshToken: string, permissions?: string[]) => void
+  logout: () => void
+  can: (permission: string) => boolean
+}
+
+const AuthContext = createContext<AuthState | null>(null)
+
+export function AuthProvider({ children }: { children: React.ReactNode }) {
+  const [token, setTokenState] = useState<string>(() => readAccessToken())
+  const [refreshToken, setRefreshTokenState] = useState<string>(() => readRefreshToken())
+
+  const claims = useMemo(() => decodeJwtClaims(token), [token])
+
+  const setTokenPair = (nextToken: string, nextRefreshToken: string, permissions?: string[]) => {
+    const cleanToken = nextToken.trim()
+    const cleanRefresh = nextRefreshToken.trim()
+
+    setTokenState(cleanToken)
+    setRefreshTokenState(cleanRefresh)
+
+    localStorage.setItem(ACCESS_TOKEN_KEY, cleanToken)
+    localStorage.setItem(REFRESH_TOKEN_KEY, cleanRefresh)
+    writePermissionKeys(permissions)
+  }
+
+  const logout = () => {
+    setTokenState('')
+    setRefreshTokenState('')
+    clearTokenPair()
+    window.location.replace('/login')
+  }
+
+  const can = (permission: string) => hasPermission(claims, permission)
+
+  const value = useMemo(() => ({ token, refreshToken, claims, setTokenPair, logout, can }), [token, refreshToken, claims])
+
+  return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>
+}
+
+export function useAuth() {
+  const context = useContext(AuthContext)
+  if (!context) {
+    throw new Error('useAuth must be used within AuthProvider')
+  }
+  return context
+}
diff --git a/student/src/index.css b/student/src/index.css
index 08a3ac9e1e5c44ce374f782d7c4fa3aa70e4c1ff..d5a9464b8787ce2b4e7078443846e981fedcc620 100644
--- a/student/src/index.css
+++ b/student/src/index.css
@@ -1,68 +1,80 @@
 :root {
-  font-family: system-ui, Avenir, Helvetica, Arial, sans-serif;
-  line-height: 1.5;
-  font-weight: 400;
+  font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif;
+  color: #111827;
+  background: #f3f4f6;
+}
+
+* {
+  box-sizing: border-box;
+}

-  color-scheme: light dark;
-  color: rgba(255, 255, 255, 0.87);
-  background-color: #242424;
+body {
+  margin: 0;
+}

-  font-synthesis: none;
-  text-rendering: optimizeLegibility;
-  -webkit-font-smoothing: antialiased;
-  -moz-osx-font-smoothing: grayscale;
+button,
+input {
+  font: inherit;
 }

-a {
-  font-weight: 500;
-  color: #646cff;
-  text-decoration: inherit;
+.login-page,
+.student-home,
+.status-page {
+  min-height: 100vh;
+  display: grid;
+  place-items: center;
+  padding: 24px;
 }
-a:hover {
-  color: #535bf2;
+
+.login-card,
+.student-home-card,
+.status-page {
+  background: #ffffff;
+  border-radius: 14px;
+  box-shadow: 0 10px 30px rgba(17, 24, 39, 0.1);
+  width: min(520px, 100%);
+  padding: 24px;
 }

-body {
+.login-card {
+  display: grid;
+  gap: 12px;
+}
+
+.login-card h1,
+.student-home-card h1,
+.status-page h1 {
   margin: 0;
-  display: flex;
-  place-items: center;
-  min-width: 320px;
-  min-height: 100vh;
 }

-h1 {
-  font-size: 3.2em;
-  line-height: 1.1;
+.login-card label {
+  display: grid;
+  gap: 6px;
+}
+
+.login-card input {
+  border: 1px solid #d1d5db;
+  border-radius: 10px;
+  padding: 10px 12px;
 }

 button {
-  border-radius: 8px;
-  border: 1px solid transparent;
-  padding: 0.6em 1.2em;
-  font-size: 1em;
-  font-weight: 500;
-  font-family: inherit;
-  background-color: #1a1a1a;
+  border: 0;
+  border-radius: 10px;
+  background: #111827;
+  color: #fff;
+  padding: 10px 14px;
   cursor: pointer;
-  transition: border-color 0.25s;
-}
-button:hover {
-  border-color: #646cff;
 }
-button:focus,
-button:focus-visible {
-  outline: 4px auto -webkit-focus-ring-color;
+
+.error-msg {
+  margin: 0;
+  color: #dc2626;
 }

-@media (prefers-color-scheme: light) {
-  :root {
-    color: #213547;
-    background-color: #ffffff;
-  }
-  a:hover {
-    color: #747bff;
-  }
-  button {
-    background-color: #f9f9f9;
-  }
+.status-code {
+  margin: 0;
+  font-size: 48px;
+  font-weight: 800;
+  color: #111827;
 }
diff --git a/student/src/main.tsx b/student/src/main.tsx
index bef5202a32cbd0632c43de40f6e908532903fd42..e8869228444574700874ffd69ad40195ae431efd 100644
--- a/student/src/main.tsx
+++ b/student/src/main.tsx
@@ -1,10 +1,13 @@
 import { StrictMode } from 'react'
 import { createRoot } from 'react-dom/client'
 import './index.css'
-import App from './App.tsx'
+import App from './App'
+import { AuthProvider } from './auth'

 createRoot(document.getElementById('root')!).render(
   <StrictMode>
-    <App />
+    <AuthProvider>
+      <App />
+    </AuthProvider>
   </StrictMode>,
 )
diff --git a/student/src/session.ts b/student/src/session.ts
new file mode 100644
index 0000000000000000000000000000000000000000..621568da16446f3a48a26ac8d49c842e9250019c
--- /dev/null
+++ b/student/src/session.ts
@@ -0,0 +1,68 @@
+export const ACCESS_TOKEN_KEY = 'aya_student_token'
+export const REFRESH_TOKEN_KEY = 'aya_student_refresh_token'
+export const PERMISSIONS_KEY = 'aya_student_permissions'
+
+export type SessionClaims = {
+  sub?: number
+  email?: string
+  role?: string
+  permissions?: string[]
+  exp?: number
+  iat?: number
+}
+
+export function readAccessToken() {
+  return localStorage.getItem(ACCESS_TOKEN_KEY) || ''
+}
+
+export function readRefreshToken() {
+  return localStorage.getItem(REFRESH_TOKEN_KEY) || ''
+}
+
+export function readPermissionKeys() {
+  try {
+    const raw = localStorage.getItem(PERMISSIONS_KEY)
+    if (!raw) return []
+    const parsed = JSON.parse(raw)
+    return Array.isArray(parsed) ? parsed.filter((item): item is string => typeof item === 'string') : []
+  } catch {
+    return []
+  }
+}
+
+export function writePermissionKeys(permissions: string[] | null | undefined) {
+  const safePermissions = Array.isArray(permissions)
+    ? permissions.filter((item): item is string => typeof item === 'string').map((item) => item.trim()).filter(Boolean)
+    : []
+
+  localStorage.setItem(PERMISSIONS_KEY, JSON.stringify([...new Set(safePermissions)]))
+}
+
+export function clearTokenPair() {
+  localStorage.removeItem(ACCESS_TOKEN_KEY)
+  localStorage.removeItem(REFRESH_TOKEN_KEY)
+  localStorage.removeItem(PERMISSIONS_KEY)
+}
+
+export function decodeJwtClaims(token: string): SessionClaims | null {
+  try {
+    const payload = token.split('.')[1]
+    if (!payload) return null
+    const normalized = payload.replace(/-/g, '+').replace(/_/g, '/')
+    const json = decodeURIComponent(
+      atob(normalized)
+        .split('')
+        .map((char) => `%${(`00${char.charCodeAt(0).toString(16)}`).slice(-2)}`)
+        .join(''),
+    )
+    return JSON.parse(json)
+  } catch {
+    return null
+  }
+}
+
+export function hasPermission(claims: SessionClaims | null | undefined, permission: string) {
+  if (claims?.role === 'ADMIN') return true
+  const merged = new Set([...(claims?.permissions ?? []), ...readPermissionKeys()])
+  return merged.has(permission)
+}
diff --git a/student/tsconfig.node.json b/student/tsconfig.node.json
index 8a67f62f4ceebff3424e6e8cd4b3c25b17347546..7a77bab3be01b708f34c334e4c4e46fa97fc7770 100644
--- a/student/tsconfig.node.json
+++ b/student/tsconfig.node.json
@@ -1,26 +1,26 @@
 {
   "compilerOptions": {
     "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.node.tsbuildinfo",
     "target": "ES2023",
     "lib": ["ES2023"],
     "module": "ESNext",
-    "types": ["node"],
+    "types": [],
     "skipLibCheck": true,

     /* Bundler mode */
     "moduleResolution": "bundler",
     "allowImportingTsExtensions": true,
     "verbatimModuleSyntax": true,
     "moduleDetection": "force",
     "noEmit": true,

     /* Linting */
     "strict": true,
     "noUnusedLocals": true,
     "noUnusedParameters": true,
     "erasableSyntaxOnly": true,
     "noFallthroughCasesInSwitch": true,
     "noUncheckedSideEffectImports": true
   },
   "include": ["vite.config.ts"]
 }
