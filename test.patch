diff --git a/Home/ayanavita-fontend-final/.env b/Home/ayanavita-fontend-final/.env
new file mode 100644
index 0000000000000000000000000000000000000000..dc27dbcb5aa7cca87f6ff295b2687edd738127cb
--- /dev/null
+++ b/Home/ayanavita-fontend-final/.env
@@ -0,0 +1 @@
+VITE_API_URL=http://localhost:8090
diff --git a/Home/ayanavita-fontend-final/.env.example b/Home/ayanavita-fontend-final/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..dc27dbcb5aa7cca87f6ff295b2687edd738127cb
--- /dev/null
+++ b/Home/ayanavita-fontend-final/.env.example
@@ -0,0 +1 @@
+VITE_API_URL=http://localhost:8090
diff --git a/Home/ayanavita-fontend-final/src/api/http.ts b/Home/ayanavita-fontend-final/src/api/http.ts
index 45371651580a823ca0b37f9959007de4f39112a7..b3aa89b3c3abe9028005c69362a6913ef4c8f125 100644
--- a/Home/ayanavita-fontend-final/src/api/http.ts
+++ b/Home/ayanavita-fontend-final/src/api/http.ts
@@ -1,19 +1,19 @@
 import axios from "axios";
 
-const baseURL = import.meta.env.VITE_API_URL ?? "http://localhost:3000";
+const baseURL = import.meta.env.VITE_API_URL ?? "http://localhost:8090";
 
 export const http = axios.create({
   baseURL,
   timeout: 20000,
   headers: { "Content-Type": "application/json" },
 });
 
 http.interceptors.request.use((config) => {
   const token = localStorage.getItem("aya_access_token");
   if (token) {
     // axios v1 headers có thể là object hoặc AxiosHeaders -> normalize
     config.headers = (config.headers ?? {}) as any;
     (config.headers as any).Authorization = `Bearer ${token}`;
   }
   return config;
 });
diff --git a/Home/ayanavita-fontend-final/vite.config.ts b/Home/ayanavita-fontend-final/vite.config.ts
index 1d8fa73113c5a29503dd6b486a844d6f467760f1..e0c03eafc6b63cf4000155f3381a99cee631d851 100644
--- a/Home/ayanavita-fontend-final/vite.config.ts
+++ b/Home/ayanavita-fontend-final/vite.config.ts
@@ -1,7 +1,10 @@
-// vite.config.ts
 import { defineConfig } from "vite";
 import react from "@vitejs/plugin-react";
 
 export default defineConfig({
   plugins: [react()],
+  server: {
+    host: "0.0.0.0",
+    port: 5176,
+  },
 });
diff --git a/backend/ayanavitabackend/app/api/src/main.ts b/backend/ayanavitabackend/app/api/src/main.ts
index 0bb67b92130df77c8041c4c19d43695865d4fb46..f2a1da3d887b653b2f8aa0fd71a7ffdcf2bace4c 100644
--- a/backend/ayanavitabackend/app/api/src/main.ts
+++ b/backend/ayanavitabackend/app/api/src/main.ts
@@ -5,50 +5,52 @@ import { DocumentBuilder, SwaggerModule } from "@nestjs/swagger";
 import { AppModule } from "./app.module";
 import { AllExceptionsFilter } from "./common/filters/all-exceptions.filter";
 
 async function bootstrap() {
   const app = await NestFactory.create(AppModule, {
     logger: ["log", "error", "warn", "debug", "verbose"],
   });
 
   // ✅ CORS – chuẩn cho cả production + admin + dev
   const allow = new Set<string>([
     // 🌍 Production main domain
     "http://ayanavita-germany.com",
     "https://ayanavita-germany.com",
 
     // 🔐 Admin subdomain
     "http://admin.ayanavita.com",
     "https://admin.ayanavita.com",
 
     // 🧪 Dev local
     "http://localhost:5173",
     "http://127.0.0.1:5173",
     "http://localhost:5174",
     "http://127.0.0.1:5174",
     "http://localhost:5175",
     "http://127.0.0.1:5175",
+    "http://localhost:5176",
+    "http://127.0.0.1:5176",
     "http://localhost:5050",
     "http://127.0.0.1:5050",
   ]);
 
   app.enableCors({
     origin: (origin, callback) => {
       // Cho phép server-to-server hoặc curl
       if (!origin) return callback(null, true);
 
       if (allow.has(origin)) {
         return callback(null, true);
       }
 
       console.error(`❌ CORS blocked for origin: ${origin}`);
       return callback(new Error(`CORS blocked for origin: ${origin}`), false);
     },
     credentials: true,
     methods: ["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"],
     allowedHeaders: ["Content-Type", "Authorization"],
     optionsSuccessStatus: 204,
   });
 
   // ✅ Global Exception Filter
   app.useGlobalFilters(new AllExceptionsFilter());
 
diff --git a/docker-compose.yml b/docker-compose.yml
index 84bbd274ffc5642f43e811d493815421f1c1cdd0..14ec190f8efff5b73be2f0bb2de173edc000b366 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -1,94 +1,103 @@
 services:
   mysql:
     image: mysql:8.0
     container_name: ayanavita-mysql
     command: --default-authentication-plugin=mysql_native_password
     environment:
       MYSQL_ROOT_PASSWORD: rootpassword
       MYSQL_DATABASE: ayanavita
       MYSQL_USER: ayanavita
       MYSQL_PASSWORD: ayanavita_pw
       TZ: Asia/Ho_Chi_Minh
     volumes:
       - mysql_data:/var/lib/mysql
     ports:
       - "3306:3306"
     restart: unless-stopped
     networks:
       - aya_net
 
-  # =====================
-  # BACKEND (DEV MODE)
-  # =====================
   backend:
     image: node:20-alpine
     container_name: ayanavita-backend
     working_dir: /app
     volumes:
       - ./backend/ayanavitabackend/app/api:/app
       - /app/node_modules
     command: sh -c "npm install && npx prisma generate && npm run start:dev"
     env_file:
       - ./backend/ayanavitabackend/app/api/.env
     environment:
       NODE_ENV: development
       PORT: 8090
     depends_on:
       - mysql
     ports:
       - "8090:8090"
     networks:
       - aya_net
 
-  # =====================
-  # FRONTEND (VITE DEV)
-  # =====================
   frontend:
     image: node:20-alpine
     container_name: ayanavita-frontend
     working_dir: /app
     volumes:
       - ./frontend:/app
       - ./frontend/landingpageayanavita:/app
       - /app/node_modules
     command: sh -c "npm install && npm run dev"
     environment:
       NODE_ENV: development
     ports:
       - "5174:5174"
     networks:
       - aya_net
 
-  # =====================
-  # ADMIN (REACT DEV)
-  # =====================
+  frontend-final:
+    image: node:20-alpine
+    container_name: ayanavita-frontend-final
+    working_dir: /app
+    volumes:
+      - ./Home/ayanavita-fontend-final:/app
+      - /app/node_modules
+    command: sh -c "npm install && npm run dev -- --host 0.0.0.0 --port 5176"
+    environment:
+      NODE_ENV: development
+      VITE_API_URL: http://backend:8090
+    depends_on:
+      - backend
+    ports:
+      - "5176:5176"
+    networks:
+      - aya_net
+
   admin:
     image: node:20-alpine
     container_name: ayanavita-admin
     working_dir: /app
     volumes:
       - ./backend/ayanavitabackend/app/admin:/app
       - /app/node_modules
     command: sh -c "npm install && npm run dev -- --host"
     environment:
       NODE_ENV: development
       VITE_API_BASE: http://localhost:8090
     ports:
       - "5050:5173"
     networks:
       - aya_net
 
   adminer:
     image: adminer
     container_name: ayanavita-adminer
     ports:
       - "8081:8080"
     networks:
       - aya_net
 
 volumes:
   mysql_data:
 
 networks:
   aya_net:
-    driver: bridge
\ No newline at end of file
+    driver: bridge
